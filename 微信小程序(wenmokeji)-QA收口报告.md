# 喷码机物联网微信小程序 — QA 收口报告

| 项目 | 内容 |
|------|------|
| 测试对象 | 喷码机物联网微信小程序（wenmokeji） |
| AppID | wx8c5a2b95965410ba |
| 基础库版本 | 3.17.0 |
| 测试日期 | 2026-08-18 |
| 测试方法 | 静态代码审查（无微信开发者工具运行环境） |
| 测试范围 | 6个页面 + 6个工具模块 + 1个自定义组件 |
| 发布结论 | **NO GO（不可发布）** |

---

## 一、测试概述

本次 QA 针对"喷码机物联网"微信小程序进行完整质量评估。由于无 PRD/原型文档，所有需求均从源码逆向分析得出；因缺少微信开发者工具和后端联调环境，功能测试以静态代码审查为主，未执行真机/模拟器交互测试。

小程序核心功能包括：用户登录鉴权、设备列表管理、设备详情、打印下发、历史记录查询、WebSocket 实时状态推送。采用 token + refreshToken 鉴权机制，支持多环境配置（dev/staging/prod），当前运行在 dev + mock 模式。

---

## 二、测试范围

### 2.1 纳入测试

| 模块 | 文件 | 覆盖说明 |
|------|------|----------|
| 小程序入口 | app.js / app.json / app.wxss | 登录态检查、版本更新、全局错误捕获、网络监听、页面路由 |
| 登录页 | pages/login/ | 账号密码登录、表单校验、登录态缓存 |
| 首页 | pages/home/ | 设备统计、设备列表、添加/删除设备、退出登录、分享 |
| 设备列表 | pages/device/ | 分页加载、下拉刷新、搜索防抖、WS实时更新 |
| 设备详情 | pages/detail/ | 详情展示、运行参数、实时时钟、WS状态同步 |
| 打印下发 | pages/print/ | 表单校验、提交防重、数据联动 |
| 历史记录 | pages/history/ | 日期筛选、分页、下拉刷新 |
| 鉴权工具 | utils/auth.js | token/refreshToken 存取、过期判断、自动刷新 |
| 请求封装 | utils/request.js | 401自动刷新、5xx重试、网络失败重试、loading |
| WebSocket | utils/websocket.js | 多页面订阅、重连退避、心跳保活、鉴权失败处理 |
| 配置管理 | utils/config.js | 多环境切换、超时/重试参数 |
| Mock数据 | utils/mock.js | 3租户隔离、运行时可变数据、8接口路由 |
| 项目配置 | project.config.json / deploy.md | 构建配置、部署指南、接口约定 |

### 2.2 不纳入测试

- 微信平台审核合规性（需提交审核后验证）
- 后端服务端逻辑与性能
- 真机兼容性矩阵（iOS/Android 多机型、微信版本）
- 微信支付等未实现功能

---

## 三、测试环境与方法

| 项目 | 说明 |
|------|------|
| 执行方式 | 静态代码审查（逐文件阅读 + 逻辑走查） |
| 运行环境 | 无微信开发者工具，无法编译运行 |
| 后端环境 | 无真实后端，mock 模式（dev 环境） |
| 证据等级 | L2_observation（代码级观察证据） |
| 用例总数 | 39 条 |
| 已执行 | 6 组静态审查（覆盖鉴权/设备管理/配置/WS/打印/全局） |
| 未执行 | 31 条功能交互用例（依赖运行环境） |
| 阻塞 | 2 条（接口路径不一致、token刷新机制失效） |
| 失败 | 2 条（生产环境配置、管理员密码安全） |

---

## 四、需求逆向分析

从源码逆向识别出 12 项核心需求：

| ID | 需求 | 优先级 | 实现状态 |
|----|------|--------|----------|
| REQ-001 | 用户登录（账号密码→缓存→跳转） | P0 | 已实现 |
| REQ-002 | 登录态保持与 token 自动刷新 | P0 | **已实现但有缺陷** |
| REQ-003 | 退出登录（清缓存+关WS+跳转） | P1 | 已实现 |
| REQ-004 | 设备列表展示（分页/搜索/刷新） | P0 | 已实现 |
| REQ-005 | 设备详情（参数展示+实时状态） | P1 | 已实现 |
| REQ-006 | 添加设备（管理员鉴权+绑定） | P1 | **已实现但有缺陷** |
| REQ-007 | 删除设备（管理员鉴权+解绑） | P1 | **已实现但有缺陷** |
| REQ-008 | 打印下发（表单+防重+联动） | P0 | 已实现 |
| REQ-009 | 打印历史记录（筛选+分页） | P2 | 已实现 |
| REQ-010 | WebSocket 实时推送（连接/重连/心跳） | P0 | 已实现 |
| REQ-011 | 网络异常处理（断网/超时/重试） | P1 | 已实现 |
| REQ-012 | 首页设备统计 | P2 | 已实现 |

---

## 五、风险机制识别

| ID | 风险机制 | 优先级 | 影响 |
|----|----------|--------|------|
| RM-001 | Token 刷新机制失效（登录未保存 refreshToken/expiresIn） | P0 | token 过期后用户被强制退出，体验中断 |
| RM-002 | 前端硬编码管理员密码 wenmo123 | P1 | 可被反编译获取，未授权添加/删除设备 |
| RM-003 | 接口路径文档与代码不一致 | P0 | 后端按文档实现将全部 404，小程序不可用 |
| RM-004 | 生产环境配置未切换（dev+mock=true） | P0 | 生产包使用假数据，无法连接真实后端 |
| RM-005 | WebSocket token 通过 URL 参数传递 | P2 | token 可能在日志/代理中泄露 |
| RM-006 | 登出未通知后端 /auth/logout | P2 | 已退出 token 仍有效，存在盗用窗口 |
| RM-007 | 打印份数校验可被 NaN 绕过 | P3 | 非数字输入绕过校验，可能导致后端异常 |
| RM-008 | 首页首次加载重复请求设备列表 | P3 | 多余网络请求，浪费流量 |

---

## 六、测试用例设计

共设计 39 条测试用例，按模块分布：

| 模块 | 用例数 | P0 | P1 | P2 | P3 |
|------|--------|-----|-----|-----|-----|
| 登录鉴权 | 8 | 2 | 4 | 1 | 1 |
| 设备管理 | 11 | 2 | 6 | 2 | 1 |
| 打印下发 | 4 | 1 | 3 | 0 | 0 |
| 历史记录 | 1 | 0 | 0 | 1 | 0 |
| WebSocket | 5 | 1 | 4 | 0 | 0 |
| 网络与全局 | 7 | 0 | 3 | 3 | 1 |
| 配置与安全 | 3 | 2 | 1 | 0 | 0 |
| **合计** | **39** | **8** | **21** | **7** | **3** |

> 完整用例清单见附件《测试方案与报告》Word 文档。

---

## 七、执行结果

### 7.1 静态代码审查执行汇总

| 执行ID | 审查模块 | 关联用例 | 发现Bug |
|--------|----------|----------|---------|
| EXE-001 | 鉴权模块（auth.js/request.js/login.js） | TC-005/006/007 | BUG-001 |
| EXE-002 | 设备管理模块（home.js/device.js） | TC-016~019/039 | BUG-002/003 |
| EXE-003 | 配置与部署（config.js/deploy.md） | TC-037/038 | BUG-003/004 |
| EXE-004 | WebSocket 模块（websocket.js） | TC-024~028 | BUG-005 |
| EXE-005 | 打印与历史模块（print.js/history.js） | TC-020~023 | BUG-006 |
| EXE-006 | 全局与网络模块（app.js/request.js） | TC-008/029~034 | BUG-007/008 |

### 7.2 用例状态统计

| 状态 | 数量 | 说明 |
|------|------|------|
| 通过 | 4 | 静态审查中逻辑结构完整的项 |
| 失败 | 2 | TC-037（生产配置）、TC-039（密码安全） |
| 阻塞 | 2 | TC-005（token刷新失效）、TC-038（接口路径不一致） |
| 未执行 | 31 | 需微信开发者工具运行环境 |

---

## 八、Bug 清单

共发现 **8 个缺陷**，其中 P0 阻断项 3 个。

### 8.1 阻断级缺陷（P0）

#### BUG-001：登录未保存 refreshToken 和 expiresIn，token 自动刷新机制完全失效

| 项目 | 内容 |
|------|------|
| 严重度 | S2 |
| 优先级 | **P0（阻断发布）** |
| 模块 | 登录鉴权 |
| 位置 | pages/login/login.js 第66行 |

**问题描述：**
登录成功后仅调用 `auth.setToken(res.token)`，未传入 expiresIn 参数，也未调用 `auth.setRefreshToken(res.refreshToken)`。导致：
1. `tokenExpireKey` 永远为 0 → `needRefresh()` 永远返回 false → 主动刷新永不触发
2. `getRefreshToken()` 返回空 → 401 时 `refreshToken()` 直接 reject → 自动刷新失败
3. 最终结果：token 过期后所有接口 401，用户被强制退出登录

**证据：**
- login.js:66 — `auth.setToken(res.token)` 无第二参数
- auth.js:128-131 — `if(!rt) return Promise.reject(new Error('无 refreshToken'))`
- auth.js:37-43 — `if(!expireAt) return false`

**修复建议：**
```javascript
// login.js 中修改为：
auth.setToken(res.token, res.expiresIn || 7200);
auth.setRefreshToken(res.refreshToken);
auth.setUserInfo(res.userInfo || {});
```
同时确认后端登录接口返回 `refreshToken` 和 `expiresIn` 字段。

---

#### BUG-003：接口路径文档（deploy.md）与代码实际调用不一致

| 项目 | 内容 |
|------|------|
| 严重度 | S2 |
| 优先级 | **P0（阻断发布）** |
| 模块 | 接口约定 |
| 位置 | deploy.md 第84-87行 vs 各页面 api 调用 |

**问题描述：**

| 功能 | deploy.md 文档路径 | 代码实际调用路径 |
|------|-------------------|-----------------|
| 设备列表 | GET /devices | GET /device/list |
| 设备详情 | GET /devices/:id | GET /device/detail |
| 打印下发 | POST /devices/:id/print | POST /device/print/dispatch |
| 历史记录 | GET /devices/:id/history | GET /device/print/history |

若后端按 deploy.md 文档实现 RESTful 接口，小程序所有数据接口将返回 404，完全不可用。

**修复建议：** 与后端开发确认实际接口路径，统一代码或文档。建议以代码实际调用为准更新 deploy.md。

---

#### BUG-004：生产发布包仍为 dev 环境且 mock=true

| 项目 | 内容 |
|------|------|
| 严重度 | S2 |
| 优先级 | **P0（阻断发布）** |
| 模块 | 配置管理 |
| 位置 | utils/config.js 第59行 |

**问题描述：**
`config.js` 中 `CURRENT_ENV = 'dev'`，对应配置 `mock: true`、`baseUrl` 为开发域名。直接上传发布将导致：
- 生产环境使用本地 mock 假数据
- 无法连接真实后端服务
- WebSocket 连接开发环境地址

**修复建议：**
发布前将 `CURRENT_ENV` 改为 `'prod'`，确认 `mock: false`、`baseUrl`/`wsUrl` 为生产域名。建议加入 CI 检查或发布前自动校验脚本。

---

### 8.2 重要缺陷（P1/P2）

#### BUG-002：添加/删除设备的管理员密码明文硬编码在前端

| 项目 | 内容 |
|------|------|
| 严重度 | S2 | 优先级 | P1 |
| 模块 | 设备管理 | 位置 | pages/home/home.js 第5行 |

**问题描述：**
`const ADMIN_PASSWORD = 'wenmo123'` 明文写在前端源码中，`verifyPassword` 函数纯前端比对。小程序代码包可被反编译，密码可被轻易获取，任何人都能通过添加/删除设备接口操作。

**修复建议：** 移除前端密码校验，改为后端接口鉴权（在 `/device/add` 和 `/device/delete` 中校验用户角色/权限）。

---

#### BUG-005：WebSocket 连接 URL 中明文携带 token

| 项目 | 内容 |
|------|------|
| 严重度 | S3 | 优先级 | P2 |
| 模块 | WebSocket | 位置 | utils/websocket.js 第173行 |

**问题描述：**
连接地址 `config.wsUrl + '?token=' + token`，token 会出现在浏览器历史、代理日志、服务器访问日志中，存在泄露风险。

**修复建议：** 改为通过 WebSocket 子协议（protocol）或连接后首条消息发送 token 鉴权。

---

#### BUG-006：打印下发份数校验可被 NaN 绕过

| 项目 | 内容 |
|------|------|
| 严重度 | S3 | 优先级 | P3 |
| 模块 | 打印下发 | 位置 | pages/print/print.js 第70行 |

**问题描述：**
`if (Number(count) < 1)` 校验中，当 count 为非数字字符串时 `Number()` 返回 NaN，`NaN < 1` 为 false，校验被绕过。

**修复建议：**
```javascript
const n = Number(count);
if (!n || n < 1 || isNaN(n)) {
  wx.showToast({ title: '喷印次数需≥1', icon: 'none' });
  return;
}
```

---

#### BUG-007：退出登录未调用后端 /auth/logout 接口

| 项目 | 内容 |
|------|------|
| 严重度 | S3 | 优先级 | P2 |
| 模块 | 登录鉴权 | 位置 | pages/home/home.js 第222-238行 |

**问题描述：**
logout 函数仅清除本地缓存和关闭 WebSocket，未调用后端登出接口。服务端 token 仍然有效，直到自然过期，存在被盗用窗口。

**修复建议：** logout 中增加 `api.post('/auth/logout')` 调用（即使失败也继续清本地）。

---

#### BUG-008：首页首次加载重复请求设备列表

| 项目 | 内容 |
|------|------|
| 严重度 | S4 | 优先级 | P3 |
| 模块 | 首页 | 位置 | pages/home/home.js 第18行、第60-63行 |

**问题描述：**
`onLoad` 中调用 `loadDeviceList()`，`onShow` 中又调用 `fetchStat() + loadDeviceList()`。小程序页面加载时 onLoad 后立即触发 onShow，导致首次进入发两次设备列表请求。

**修复建议：** onLoad 中不调用 loadDeviceList，统一由 onShow 处理；或增加首次加载标记位。

---

### 8.3 Bug 统计

| 优先级 | 数量 | Bug ID |
|--------|------|--------|
| P0（阻断） | 3 | BUG-001, BUG-003, BUG-004 |
| P1（重要） | 1 | BUG-002 |
| P2（一般） | 2 | BUG-005, BUG-007 |
| P3（轻微） | 2 | BUG-006, BUG-008 |
| **合计** | **8** | |

| 严重度 | 数量 |
|--------|------|
| S2 | 4 |
| S3 | 3 |
| S4 | 1 |

---

## 九、发布结论

### 结论：NO GO（不可发布）

### 判定理由

1. **存在 3 个 P0 阻断缺陷，均未修复：**
   - BUG-001：token 自动刷新机制完全失效，用户体验中断
   - BUG-003：接口路径前后端不一致，存在全量 404 风险
   - BUG-004：生产环境配置未切换，将使用 mock 假数据

2. **无任何真机功能测试证据：** 39 条用例中仅 6 组静态审查有结论，31 条核心功能交互用例未执行，关键用户旅程（登录→设备列表→详情→打印→历史）未经验证。

3. **安全风险未消除：** 前端硬编码管理员密码、WS token 明文传输、登出不通知后端等问题。

### 发布前置条件

| 序号 | 条件 | 对应Bug | 责任人 |
|------|------|---------|--------|
| 1 | 修复登录保存 refreshToken/expiresIn，回归 token 过期自动刷新场景 | BUG-001 | 前端 |
| 2 | 统一前后端接口路径，确认后端实现并联调通过 | BUG-003 | 前后端 |
| 3 | 发布前切换 CURRENT_ENV=prod, mock=false，验证生产域名连通性 | BUG-004 | 前端/运维 |
| 4 | 在微信开发者工具中完成 P0/P1 功能用例执行（至少 TC-001/004/005/009/014/017/020/024/036） | — | 测试 |
| 5 | 移除前端硬编码密码，改为后端鉴权（建议） | BUG-002 | 前端/后端 |

---

## 十、未覆盖范围

以下范围因环境限制未验证，需在后续测试中补充：

1. **所有 UI 交互功能**：36 条用例依赖微信开发者工具运行环境，包括登录流程、页面跳转、表单交互、列表滚动等
2. **真机兼容性**：iOS 12+ / Android 主流机型（华为、小米、OPPO、VIVO）、微信版本 7.0+
3. **后端接口联调**：无 staging/prod 环境，无法验证真实接口响应
4. **WebSocket 真实连接**：仅 mock 模式验证逻辑结构，未验证真实 wss 连接、重连、心跳
5. **微信平台审核合规性**：用户协议、隐私政策、类目资质、违规内容检查
6. **性能指标**：首屏加载时间、小程序包大小、内存占用、CPU 消耗
7. **并发场景**：多请求并发 401 刷新、快速连续操作、WS 消息风暴

---

## 十一、后续测试建议

### 11.1 立即执行（修复 P0 后）

1. 在微信开发者工具中导入项目，mock 模式下执行全量功能用例
2. 重点回归：token 过期自动刷新、添加/删除设备流程、打印下发数据联动
3. 切换 staging 环境与后端联调，验证所有接口路径一致性

### 11.2 发布前必须完成

1. 生产环境配置切换验证（env=prod, mock=false）
2. 真机测试（至少 iOS + Android 各一台主流机型）
3. 弱网/断网场景测试
4. 微信公众平台合法域名配置确认
5. 小程序包大小检查（<2MB，超出需分包）

### 11.3 热修复回归策略

修复 BUG-001（token刷新）后需回归：
- TC-005：token 过期后 401 自动刷新
- TC-006：多请求并发 401 只刷新一次
- TC-007：refreshToken 过期后跳登录

修复 BUG-003（接口路径）后需回归：
- 所有页面数据加载接口（TC-009/014/023 等）

修复 BUG-004（环境配置）后需回归：
- TC-037：生产环境配置检查
- 全量接口连通性

---

## 十二、本轮披露

以下为门禁校验中识别的待改进项（不阻断发布，建议后续优化）：

- P0/P1 需求建议补充结构化业务行为拆解和影响范围说明
- 开放问题建议补充最小确认动作（Q-001~Q-004）
- 建议补充测试账号清单和测试数据准备说明
- 建议增加用例的前置条件和测试数据详细描述

---

*报告生成时间：2026-08-18 | 测试负责人：豆包 QA | 证据等级：L2_observation*
